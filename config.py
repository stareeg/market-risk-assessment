"""
Конфигурация загрузки данных для задания по рыночному риску (пункт 1).

Здесь собраны ВСЕ настройки эксперимента, чтобы загрузка была полностью
воспроизводимой: период, список инструментов, выбранный торговый день для
срочного рынка и т.п. Менять состав данных нужно только здесь.
"""
from __future__ import annotations
from pathlib import Path

# Каталоги. config.py лежит в корне проекта, рядом с ним папка data.
PROJECT_DIR = Path(__file__).resolve().parents[0]
DATA_DIR = PROJECT_DIR / "data"
DATA_DIR.mkdir(parents=True, exist_ok=True)

# --- Период истории (п. 1.a–1.e) -----------------------------------------
# Задание: с 1 января 2021 г. по 1 января 2026 г.
START_DATE = "2021-01-01"
END_DATE = "2026-01-01"

# Воспроизводимость: фиксируем seed (на этапе загрузки не используется,
# но требование задания — фиксировать его во всём пайплайне).
RANDOM_SEED = 42

# --- Облигации (п. 1.b, 1.c) ---------------------------------------------
# Критерии задания: ОФЗ с полностью известными выплатами (фиксированный купон,
# не привязанный к показателям), без оферт, со сроком погашения после
# 1 января 2026 г.  Этим критериям отвечают выпуски ОФЗ-ПД (постоянный доход).
#
# Требуется 5 штук — берём 10 с запасом и так, чтобы они покрывали кривую
# доходности от коротких до длинных сроков (это пригодится для PCA в п. 2).
# Указаны короткие номера; полный SECID (вида SU26221RMFS0) резолвится в коде.
OFZ_PD_NUMBERS = [
    "26207",  # погашение 2027
    "26212",  # 2028
    "26219",  # 2026
    "26226",  # 2026
    "26224",  # 2029
    "26228",  # 2030
    "26218",  # 2031
    "26221",  # 2033
    "26225",  # 2034
    "26230",  # 2039 (длинный конец кривой)
]

# --- Акции (п. 1.d) -------------------------------------------------------
# Требуется 10 — берём 12 ликвидных «голубых фишек» с устойчивым тикером на
# всём интервале 2021–2026 (без переименований вроде YNDX->YDEX).
STOCK_TICKERS = [
    "SBER",  # Сбербанк, ао
    "GAZP",  # Газпром
    "LKOH",  # ЛУКОЙЛ
    "GMKN",  # Норникель
    "ROSN",  # Роснефть
    "TATN",  # Татнефть, ао
    "NVTK",  # НОВАТЭК
    "MGNT",  # Магнит
    "MTSS",  # МТС
    "PLZL",  # Полюс
    "SNGS",  # Сургутнефтегаз, ао
    "CHMF",  # Северсталь
]

# --- Индексы (п. 1.e) -----------------------------------------------------
INDEX_TICKERS = {
    "IMOEX": "Индекс МосБиржи (рублёвый)",
    "RTSI": "Индекс РТС (долларовый)",
}

# --- Валюта (п. 1.e) ------------------------------------------------------
# Официальные курсы ЦБ РФ. Коды валют в сервисе XML_dynamic ЦБ.
CBR_CURRENCIES = {
    "USD": "R01235",  # Доллар США
    "EUR": "R01239",  # Евро
}

# --- Нефть Brent (п. 1.e) -------------------------------------------------
# Спот Brent в открытом доступе MOEX/ЦБ не отдают, поэтому строим непрерывный
# ряд фронт-месяца из фьючерсов BR (FORTS, moex.ru): на каждый день берём
# контракт с максимальным дневным объёмом торгов.
BRENT_ASSETCODE = "BR"

# --- Срочный рынок: фьючерс + опционы (п. 1.f) ----------------------------
# Один выбранный актив из п. 1.e и один торговый день 2025 г.
# Выбран фьючерс на доллар США (Si) — самый ликвидный опционный рынок на MOEX,
# а доллар присутствует в портфеле (п. 1.e).
# Дата 2025-10-15 выбрана так, чтобы ближайший квартальный фьючерс (декабрь,
# экспирация ~18.12.2025) отстоял более чем на 1 месяц (требование задания).
FORTS_ASSETCODE = "Si"
FORTS_TRADE_DAY = "2025-10-15"
FORTS_MIN_DAYS_TO_EXPIRY = 31  # «не ближе, чем 1 месяц»
